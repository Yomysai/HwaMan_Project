/**
 * 
 */
package fr.freestyle.hawman.Controller;

import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import fr.freestyle.hawman.Service.UserService;

/**
 * @author aly_d
 *
 */
@RestController
@RequestMapping("/users")
@CrossOrigin(origins = "http://localhost:3000")
public class UserController {
    private final UserService userService;

    public UserController(UserService userService) {
        this.userService = userService;
    }

    // Méthodes pour les opérations CRUD sur les utilisateurs
    // Exemple : create, read, update, delete

    // Autres méthodes pour les fonctionnalités spécifiques liées aux utilisateurs
}

